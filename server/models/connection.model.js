class ConnectionModel {
  constructor (name, urlData, requestData, responseData) {
    this.name = name
    this.urlData = urlData
    this.requestData = requestData
    this.responseData = responseData
  }
}

module.exports = ConnectionModel
