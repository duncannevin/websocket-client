const UserRepository = require('./user.schema')
const ConnectionRepository = require('./connection.schema')
const SocialRepository = require('./social.schema')

module.exports = {UserRepository, ConnectionRepository, SocialRepository}
