const UserRepository = require('./user.schema')
const UrlRepository = require('./url_data.schema')
const RequestDataRepository = require('./request_data.schema')
const ResponseDataRepository = require('./response_data.schema')

module.exports = {UserRepository, UrlRepository, RequestDataRepository, ResponseDataRepository}
